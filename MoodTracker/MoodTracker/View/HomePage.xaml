<Page x:Class="MoodTracker.View.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:MoodTracker.View"
      xmlns:controls="clr-namespace:MoodTracker.Controls"
      Title="HomePage">

    <!--<Border Grid.Column="1"
                    Background="{StaticResource ContentSurface}"
                    CornerRadius="40"
                    Margin="0"
                    BorderBrush="#30324A"
                    BorderThickness="1">-->

        <Grid
            Background="{StaticResource ContentSurface}"
            
            >
            <!-- 主内容三列布局 -->
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="360*"/>
                    <!-- 左侧 -->
                    <ColumnDefinition Width="500*"/>
                    <!-- 中间 -->
                    <ColumnDefinition Width="300*"/>
                    <!-- 右侧 -->
                </Grid.ColumnDefinitions>

                <!-- 左侧： RecordList -->
                <controls:RecordList Grid.Column="0" Margin="0,0,10,0"/>

                <!-- 中间 -->
                <Border Grid.Column="1" Background="Transparent">
                    <controls:MusicDisplay Grid.Column="1"/>
                </Border>

                <!-- 右侧 -->
                <Border Grid.Column="2" Background="Transparent">
                    <controls:RightSidebar Grid.Column="2"/>
                </Border>
            </Grid>
</Page>
